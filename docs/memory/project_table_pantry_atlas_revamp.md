---
name: project_table_pantry_atlas_revamp
description: "2026-07-03 revamp — phases 1 + 1.5 + mobile port SHIPPED; phase 2 Pantry steps 1-3 merged, 6/9 art assets ingested on revamp/phase-2-art, steps 4-8 in progress; REMIND USER: honey, garlic, lamb renders still owed; phase 3 journal later"
metadata: 
  node_type: memory
  type: project
  originSessionId: e95f5e8e-4319-4f1a-ac0c-b2c5ffe50fc2
---

Approved 2026-07-03: full catalog revamp, spec at `docs/superpowers/specs/2026-07-03-table-pantry-atlas-revamp-design.md` (gitignored, local-only). Supersedes map-as-homepage and the TODO.md "Prophetic Foods page" sketch.

Locked decisions:
- Editorial home: masthead, curated Table spread hero (a spread may be a single dish; `data/table-spreads.ts`), collections row, Pantry shelf teaser, latest.
- Map moves whole to `/atlas`; nav becomes Home · Recipes · Pantry · Atlas · About · Halal.
- Data: `influences text[]` (plural, world-atlas names); existing `country` column re-semanticized as primary influence (only it stamps); country/region nullable for origin-less recipes (absent from atlas, no country stamp — correct, not a gap); `category` (main/side), `is_sunnah`, `featured_ingredients`.
- **Sunnah is a thread, not a headline**: always sealed + cited wherever it appears, never promoted first; Pantry is the everyday pantry with prophetic entries marked; dedicated Sunnah page deferred (a curation layer over the same pantry data, revisit once marked Pantry is live). Citation type-required whenever `prophetic` exists.
- Health voice: protein chip as quiet supporting detail (threshold constant in `lib/collections.ts`, initial 25g/serving), filterable, never the headline.
- Progression future: **Cook's Journal contains the passport** as its travel chapter (phase 3, own design pass). User explicitly rejected keeping passport-with-appendix as the end state. Phase 1: null-country cooks log as `new_recipe` tier.
- Atlas seamlessness bar: van Wijk/`d3.interpolateZoom` camera (replacing linear lerp in `zoomTo`), fix the rAF/onMoveEnd feedback loop, gesture-cancels-animation, fade-not-pop layers, skeleton, reduced-motion. Phase 1.5, isolated from the IA pivot.
- Phasing: 1 pivot → 1.5 atlas feel → 2 pantry → 3 earned layer (Table remix once sides exist, Journal). Design-once-ship-phased; user chose this over big-bang.

**Phase 1.5 SHIPPED 2026-07-04** (merged to main, fast-forward to `a47bcf8`, not pushed): van Wijk flights (`lib/map/camera.ts`, 550ms cap — user tuned down from 700), settle handshake killing the max-update-depth bug (RSM v3 swallows onMoveEnd on prop moves; ~2-frame timer closes the window), zoom floor = reset = 1.05 (dead zone below world-fills-frame removed; pan clamp used to invert there), overlapping fade bands, skeleton, mount fades, reduced motion, mobile follow via `useMobileMapPosition`. Shelf header shipped same day: variant B from `/dev/shelf-header` (no numeral, bigger type, more air; `COLLECTION_ACCENTS` in lib/collections.ts). UX critique scored atlas 24/40; mechanics fixes shipped, structural drill-down prototypes evaluated and user kept ALL THREE — baked into desktop at `e75779b` (markers at continent zoom everywhere, see-all bridge pill, continent click reveals continent panel; flags stripped). NEXT task: port all three to mobile (bottom-sheet model), then phase 2 Pantry.

**Mobile drill-down port SHIPPED 2026-07-04** (commit `0402933`, not pushed; earlier history through `e75779b` IS pushed): dots visible from world view (`M_ZOOM.DOT_FADE_IN/FULL` ≤ 0.95 floor), dot tap flies + opens country sheet, bridge pill above the rail (hidden while a sheet is open), region taps (rail chip / double-tap dive / land tap below label zoom) open a collapsed region peek sheet, expandable via grid-rows 0fr→1fr (padding must live on the inner div or the 0fr track leaks). Browser-verified both themes via playwright-core + system Chrome. P3 stale-crumb split into its own TODO item.

**Phase 2 Pantry design pass DONE 2026-07-04** (commit `9fcd3a5`): sandbox at `/dev/pantry` — card variants A specimen / B ledger / C loose × seals rosette / octagon / scallop (recommended A + rosette), one entry-view proposal, 9-entry launch batch (4 prophetic: barley, dates, honey, olive oil) with draft citations that MUST be verified on sunnah.com before ship. Design doc `docs/superpowers/specs/2026-07-04-pantry-design.md` (local-only); implementation plan `docs/plans/2026-07-04-pantry-plan.md` (committed). **DESIGN APPROVED same session, all picks resolved**: card = variant D "etched" (A x C hybrid: outline card, no filled surface), seal = ROSETTE (confirmed against 7 candidates incl. shamsa + girih star, commit `d571ac6`), ﷺ = inline glyph. User flagged placeholder art quality (lamb) — expected, real flat-ink assets come via the Sora pipeline; lamb redrawn as a frenched chop. Plan is UNBLOCKED: next session executes docs/plans/2026-07-04-pantry-plan.md top to bottom (step 1 includes sunnah.com citation verification). Phase 3 not started, per instruction.

**Phase 2 steps 1-3 SHIPPED + MERGED 2026-07-04** (main fast-forwarded b580fdb..fe32b5a, 5 commits, SDD per-task reviews + final whole-branch review clean, branch deleted; main NOT pushed): `data/pantry/` (9 entries, 4 prophetic, every citation independently re-verified — sunnah.com Cloudflare-blocks direct fetch, Wayback sunnah.com + quran.com is the working path); dates note REWORDED (draft negatively inverted Muslim 2046a's positive wording — corrected, dual-cited, user voice-check pending); `featuredIngredients` live on 4 recipes (aglio-e-olio, prawn-spaghetti, turkish-eggs, xinjiang) + reseeded/REST-verified, both lasagnas deliberately empty; nine Sora prompts at `docs/plans/2026-07-04-pantry-art-prompts.md` (committed) AWAITING USER RENDERS (batches of 3; PNGs land in ~/Downloads named <slug>.png). Steps 4-6 (/pantry route, navbar, home teaser, FilterPanel chips) ship only WITH the rendered art, on a fresh branch off main. Step-4 session notes: shelf renderer must iterate KIND_ORDER (don't trust PANTRY array order); add seed-time featuredIngredients-vs-PANTRY slug validation; add `public/pantry/` to AUTO_CONVERT_DIRS when the first asset lands.

**Phase 2 steps 4-8 SHIPPED 2026-07-05** (branch `revamp/phase-2-art`, NOT merged — user must approve merge; 5 commits `27dac4a`..`9ae7ac0`): `/pantry` route (server shell → client `PantryShelf` variant D etched + rosette seal, entry overlay with focus trap/Escape/backdrop), navbar link (both `Navbar` + `NavMenuDropdown`, between Recipes and Atlas), home `PantryTeaser` (between Collections and Latest), FilterPanel Collections chips (URL presets, not filter state, so no badge bump), seed-time `featuredIngredients`-vs-`PANTRY` validation. Home page became a Server Component; `TableSpreadHero`+`LatestFromKitchen` now self-fetch via `useRecipes`. `lib/pantry/landed.ts#landedPantryEntries()` is the shared fs source of truth: shelf + teaser show ONLY entries whose art landed (no placeholder art in prod), so the last three appear automatically when rendered. **ALL 9/9 art ingested** (2026-07-05: honey + lamb native alpha, garlic was a white-bg Photos JPEG keyed at 8% fuzz; all alpha>16 crop, cwebp q82) — shelf + teaser now complete, meat & fish group populated. **`/addpantry` skill built** (`.claude/skills/addpantry/`) for future pantry entries: data entry + art ingest (alpha>16 crop, PIL key since magick missing) + optional featuredIngredients wiring; application-tested. Verified: tsc + build clean, headless-Chrome drove both themes 29/31 (2 non-passes = env console noise: Supabase 401 headless-Turnstile + external DNS). Phase 3 (Cook's Journal) NOT started, per instruction.

**Phase 1 SHIPPED 2026-07-03**: merged to main locally (fast-forward to `21ff4fe`, 14 commits, all SDD-reviewed + final whole-branch review clean), main NOT pushed to origin yet. Live DB migrated + reseeded. Home collections use the "chapter card" treatment (user-picked hybrid: chapter typography inside accent-bar card; sandbox at `/dev/home-collections` kept). Collections on /recipes present as shelf headers, not filters (never re-add to badge/chips). Deferred polish + phase 1.5 items live in TODO.md and the parked-findings triage in `.superpowers/sdd/progress.md`. Next queued: TODO item "Collection shelf header design pass" (user's nit), then phase 1.5 atlas pass.

Related: [[project_business_validation_strategy]], [[feedback_halal_trust_voice]], [[feedback_per_region_enrichment]], [[reference_todo]].
