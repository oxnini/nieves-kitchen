# Premium revamp · every ruling (R1–R133)

Collected from the SDD ledger for the 9-phase premium revamp (PRs #9–#17, spec `docs/superpowers/specs/2026-09-25-premium-revamp-design.md`) when the plan finished on 2026-09-26. Each line: what was decided — why — cost if wrong. Historical record; the code and spec win where they differ.

- Ruling: work on branch revamp/p1-foundation in the main checkout, no worktree — the user's prompt names the branch workflow explicitly — cost if wrong: none, the checkout is clean.
- Ruling R1: Task 1.3 also retints the hard-coded cobalt-night literals in the [data-theme="sepia"] built-in overrides (and .text-ink night lift) to their night-teal equivalents from spec §3.2 — spec §3 is a value-level remap and leaving cobalt literals would paint blue at night on a teal page — cost if wrong: a few overrides need re-tuning in later phases; trivially revertible.
- Ruling R2: Screenshots for Phase 1 are taken by the controller after Task 1.3 (dev server is shared; build and dev can't run together without rm -rf .next) — cost if wrong: none.
- Ruling: shoot.mjs waits for 'load' + 3s instead of 'networkidle' — Turnstile's challenge iframe keeps a blob request pending so networkidle never fires on localhost — cost if wrong: a late-loading image could be missing from a shot; each shot is eyeballed anyway.
- Ruling R3: fix it in globals.css with targeted sepia overrides (dark ink #1A2B2D on bg-terracotta/bg-paprika when paired with text-white/text-cream, compound + descendant, incl. focus:bg-terracotta) per CLAUDE.md's 'targeted override' rule, not per call site — spec §3.2 lifts terracotta at night but is silent on the text on it — cost if wrong: dark text on light terracotta at night may read heavier than the designer wants; later phases can restyle per surface.
- Ruling R4: atlas choropleth stays cobalt-blue until phase 6 (lib/regions.ts is phase 6 scope in the plan); terracotta eyebrows on teal home tiles stay until phase 3 deletes the tiles — cost if wrong: user sees off-palette blue on /atlas and faint eyebrows on / during phases 1-5 review.
- Ruling R5: fix (1)-(3) in phase 1 with CSS-only targeted overrides in globals.css (descendant pins under .bg-cobalt / .bg-cobalt-deep for text-brass/outline-brass/bg-brass; .bg-brass.text-cobalt-deep; sepia .text-olive → brown-medium with .bg-cream re-pin) rather than pulling phase-2 component edits forward — keeps phase scopes intact while leaving p1 shippable (plan: every phase must leave the site shippable); phase 2/3/7 restyles supersede them — cost if wrong: a few overrides become dead CSS for phase 9 to delete.
- Ruling R6: R4 amended — home-tile eyebrows are invisible (1.05:1), not faint; R5's .bg-cobalt descendant pin covers them — cost if wrong: none.
- Ruling R7: branch in the main checkout, no worktree — user's prompt names the branch workflow — cost if wrong: none, checkout clean.
- Ruling R8: Eyebrow TONE map unchanged (brass -> text-brass, olive -> text-olive) — spec's "brass reads terracotta / olive reads muted ink" is already true by value; switching brass to text-terracotta would escape the .bg-cobalt bridge on ThisWeek's tile and make it invisible (1.05:1) until phase 3 — cost if wrong: a later phase changes one line.
- Ruling R9: Task 2.1 also retints Chip's inactive inset ring from old-cobalt rgba(32,64,107,·) to teal rgba(51,118,119,·), same alphas — spec §3 is a value-level remap and the literal escapes it — cost if wrong: trivial, chip is on the phase-3-replaced hero only.
- Ruling R10: bridge retirement in globals.css happens in Task 2.3 (after navbar and footer both moved), each rule kept or removed by grep of app/ + components/ excluding app/dev — cost if wrong: a dead rule survives to phase 9 or a live one is lost (caught by the night screenshots).
- Ruling R11: if "hidden sm:inline-flex" on Button loses to the base inline-flex at 390, wrap the Button in <span className="hidden sm:inline-flex"> instead of changing Button's API — cost if wrong: one extra span.
- Ruling R12: implementers run npm run typecheck only; the controller stops dev, runs npm run build, rm -rf .next, restarts dev, shoots screenshots, and pushes/opens the PR after the final review (same as R2) — cost if wrong: none.
- Ruling R13: add a sepia lift `[data-theme="sepia"] .focus-visible\:outline-teal:focus-visible { outline-color: #8CC3C1 }` beside the existing .text-teal night split in globals.css — keeps the plan's outline-teal class and mirrors how .text-teal is already lifted; fixes every outline-teal site-wide — cost if wrong: one CSS rule to retune.
- Ruling R14: give the Button primitive a base focus ring `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-teal` (lifted at night by R13) — spec §5 is silent on focus; a CTA with no visible focus is an a11y regression — cost if wrong: a ring on every Button that designers may want restyled.
- Ruling R15: give the compact nav passport icon a CSS-only night plinth (round bg-plinth disc behind the image under [data-theme="sepia"], no plinth by day) — spec's ink-art rule (no plinth by day, plinth at night, CSS only) — cost if wrong: a small cream disc in the night nav the user may want restyled.
- Ruling R16: fix NavMenuDropdown to return focus to the trigger only on an open→closed transition (track the previous open value in a ref), not on mount — restores the effect's stated intent; the plan's "don't touch NavMenuDropdown wiring" protects behaviour, and this is the behaviour it meant — cost if wrong: none; open/close/Escape/focus-trap unchanged.
- Ruling R17: fix the night primary hover with an unlayered sepia rule lifting `.bg-teal.hover:bg-cobalt-deep:hover` to #337677 (day teal) beside the night-teal split — moves the hover away from the dark ground, 4.87:1 with cream — cost if wrong: one colour to retune.
- Ruling R18: fold the cheap minors into the same fix wave (hairline as box-shadow so the band stays 64/88px; reword the three stale comments plus the Footer and chip-on comments; motion-reduce:hover:translate-y-0 on primary/accent) — the user reviews visually and the 1px overlap/comments are trivial now, costly to remember later — cost if wrong: none.
- Ruling R19: navbar element order (flex-1 spacer pushes links right; Start cooking + menu after the pod) left unchanged — pre-existing, plan asked for no layout change; surfaced to the user for their visual review — cost if wrong: a small layout edit in a later pass.
- Ruling R20: keep the plan workspace/ledger (don't delete) — plan has phases 3-9 left and the user asked to continue this ledger across phases — cost if wrong: a few KB of gitignored scratch.
- Ruling R21: Journal text link keeps the prefetch-on-idle/hover behaviour by moving PassportAffordance's prefetch into a small hook; only assets /journal actually renders are kept in the list (booklet wallpapers/cover are retired surfaces) — cost if wrong: first Journal visit loads stamps a bit later, or a few extra prefetches.
- Ruling R22: footer links match the configurator footer (Recipes · Atlas · Pantry · Journal · The halal promise · About) — follows from "Halal footer only" and the configurator — cost if wrong: one array edit.
- Ruling R23: band height stays 64/88 (spec), not the configurator's 68 — avoids touching <main> padding — cost if wrong: band a little taller than the mock.
- Ruling R24: run Phase 3 as ONE implementer dispatch covering Tasks 3.1-3.3 (one commit per task) and one task review over the whole range — 3.1 cannot be verified until 3.3 wires it, the three touch ~6 small files with no parallelism, and one reviewer seat sees the composed page — cost if wrong: a larger review diff; per-task commits keep it separable.
- Ruling R25: work on revamp/p3-home in the main checkout (no worktree), like phases 1-2 (R7) — cost if wrong: none.
- Ruling R26: Task 3.3 also retires globals.css bridges by grep of app/ + components/ excluding app/dev: `.bg-cobalt .text-brass` (remove if no match), `.bg-cobalt-deep .text-brass` (keep: PantryShelf), `[data-theme=sepia] .text-olive` base lift (keep while PantryShelf matches), `.bg-cream .text-olive` re-pin (remove unless grep finds a live site), and the night terracotta dark-ink block: keep the compound rules still matched (Button accent, CookedButton, StickyStepCard, skip link), drop the descendant alpha variants (`.text-cream/85`, `/90`, paprika `.text-white/80`) and plain descendant rules only if nothing live matches; comments updated to name the survivors — the user's prompt requires it — cost if wrong: a night contrast regression somewhere, caught by night shots.
- Ruling R27: keep the plan's lg (1024px) switch and the sm 480px image step rather than the configurator's 900px — Tailwind has no 900 step, and 480px on tablets reads as the same hero; at the two review widths (390, 1280) plan and configurator match — cost if wrong: the plate sits under the image from 900-1023px, one breakpoint edit.
- Ruling R28: section h2 uses the configurator's `text-[clamp(2rem,3.4vw,2.8rem)]` and the card grid the configurator's `gap-x-7 gap-y-11` (28/44px), not the plan's values — the configurator is the visual source of truth (phase-2 lesson) — cost if wrong: two class strings.
- Ruling R29: Task 3.3 updates CLAUDE.md's `/` route row (and any line naming the deleted home components) to the new composition — keeps the repo's architecture doc truthful, as phase 2 did — cost if wrong: one doc line.
- Ruling R30: the review's Important "hero asset 1536px < 2400px" (plan-mandated) ships as-is in phase 3 and is disclosed in the PR — spec §15.2 says the ≥2400px file is needed before launch, not before phase 3 — cost if wrong: a soft hero on large/retina screens until the user supplies a re-render or approves an upscale.
- Ruling R31: one fix wave for minors 1, 2, 3, 5 and 8 (hover + teal focus ring on the home text links, singular/zero-country copy, count shown only once recipes load successfully, `sm:first:pl-0`) — cheap and visible to a user who reviews by eye, same reasoning as R18 — cost if wrong: a few class strings.
- Ruling R32: minors 4 (skeleton height) and 6 (plate under extreme text zoom) wait for phase 4 / accept — phase 4's 3:2 ruled card changes card height anyway; the copy is fixed — cost if wrong: small below-fold CLS until phase 4.
- Ruling R33: minor 7 (link "Every recipe is halal" to /promise) NOT done; surfaced to the user — spec §6.1 describes it as a label, and /promise stays reachable from the footer, /about and /recipes (§13 holds) — cost if wrong: one Link wrapper.
- Ruling R34: resolve that residual with one line in the existing night teal-lift block (`[data-theme="sepia"] .group:hover .group-hover\:text-teal` → #8CC3C1, matching Tailwind v4's emitted selector), resuming the same implementer, and verify it by a controller computed-style probe at night instead of a second review seat — it's a regression our own fix wave introduced, the fix is one line in a block of identical siblings, and the skill's no-second-wave rule exists to stop churn, not to ship a known contrast bug — cost if wrong: one CSS line.
- Ruling R35: keep the plan workspace/ledger (as R20) — phases 4-9 remain — cost if wrong: a few KB of gitignored scratch.
- Ruling R36: meta line = configurator (13px sentence case, text-brown-medium, spans separated by a 4px rotated terracotta diamond: place, [Fusion], time) with calories right-aligned in text-brown-medium/80 — user pick; brown-light (#8BA19E, 2.6:1 day) is too faint to read, /80 gives ~3.9:1 day and ~6:1 night — cost if wrong: calories fail AA 4.5 for small text by day; one class to darken.
- Ruling R37: collection chip row on /recipes = "All recipes" + COLLECTIONS with includes !== null (high-protein, sides, sunnah last), links that set/clear ?collection= while keeping ?q= and ?country=; Chip primitive restyled to the configurator (transparent, 1px line inset ring, text-brown-dark 14px regular, px-[15px] py-2) since it has no live call site; selected keeps chip-on night fill — user pick + configurator — cost if wrong: a chip row the user can delete.
- Ruling R38: uniform grid on /recipes (featured prop no longer passed); RecipeCard keeps the featured prop working in the ruled style per plan — user pick — cost if wrong: one prop.
- Ruling R39: card overlays (heart, Cooked) become solid paper pills (bg-surface, ring-1 ring-line, no /90, no backdrop-blur); Cooked moves to the photo's top-left where the country pill was, keeping its terracotta fill — "overlays are paper, never glass" and the meta line has no room — cost if wrong: badge position, one class string.
- Ruling R40: /recipes controls row = configurator: no border-t rule above; search, FilterPanel inline trigger and sort select all bg-surface + 1px line ring (ring-1 ring-line, no shadow), rounded-full, same 44px height; FilterPanel inline label becomes body-font 14.5px sentence-case "Filters" (inline variant only; fab/map are phase 6 atlas); badge count unchanged; sort select stays (§13) — cost if wrong: the atlas filter trigger reads differently from /recipes until phase 6.
- Ruling R41: loading skeletons on /recipes, /favorites and CookSomethingNew match the ruled card (aspect-[3/2] block, rule, meta/title/blurb bars, no card surface); /favorites and /recipes grids use the home grid gaps `gap-x-7 gap-y-8 sm:gap-y-11` (configurator 44/28, 32 on phones) — cost if wrong: gap values.
- Ruling R42: extract hooks/useCookedRecipeSlugs.ts (Set of cooked slugs from useCookedStamps) and use it in CookSomethingNew and /recipes — Phase 3 deferral — cost if wrong: none.
- Ruling R43: run Phase 4 as ONE implementer dispatch (commits 4.1 card+skeletons, 4.2 page+chips+hook) and one task review over the range, like R24 — ~6 small files, sequential — cost if wrong: larger review diff.
- Ruling R44: h1 keeps "The recipes so far" + eyebrow + lead (spec: "or the page's existing heading"), weight font-normal; the shelf header h2 font-normal too — cost if wrong: copy.
- Ruling R45: /favorites h1 'My Favorites' goes font-normal like /recipes (global constraint: headline weight 400; /favorites is a §7 surface) — folded into the final fix wave — cost if wrong: one class. Copy 'My Favorites' (title case) left as-is, surfaced to user.
- Ruling R46: one fix wave (resume implementer) for 1, 2, 3 and minors 4 (/80→/90, supersedes R36's shade), 5 (focus:ring-2 focus:ring-teal + night lift line), 6 (debounce reads window.location.search), 7 (Chip passes scroll={false}), 8 (role=group aria-label="Collections"; aria-current left as page), 9 (ring-line only when inactive), 10b (sort select text-base sm:text-sm); 10a heading skip left (pre-existing, visual h2 would change the design) — cheap, and the user reviews visually and with keyboard — cost if wrong: a few class strings.
- Ruling R47: keep the plan workspace/ledger (as R20/R35), phases 5-9 remain — cost if wrong: a few KB of gitignored scratch.
- Ruling R48: header (eyebrow, title, attribution, facts, lede, InfoStrip, equipment, Start cooking) sits ABOVE the raised page; the raised page holds only Ingredients | Method; plan's action positions; tips = tinted box in back matter — user picks — spec §8 amended in 505396f — cost if wrong: the page reads less like the configurator's single sheet; a layout move to redo.
- Ruling R49: facts row = Total, Active, Rest (only when > 0), Difficulty; "serves N" leaves the facts row (the stepper reads "Serves N", configurator) — spec + configurator over plan — cost if wrong: one fact cell.
- Ruling R50: eyebrow = country · region (· Fusion when isFusion; region omitted when null); attribution line = attribution, then "Inspired by X, Y" when inspiredBy is set, joined with " · " — keeps every piece of the old heroMeta — cost if wrong: copy placement.
- Ruling R51: ingredients controls restyled to the configurator: pill stepper (rounded-full, ring-1 ring-line, 30px round −/+ buttons, "Serves N" text, same aria-labels and disabled bounds) and a segmented Metric | US unit toggle (rounded-full, aria-pressed, selected = bg-brown-dark text-parchment); label stays "US" because lib/units converts to US customary, not imperial — cost if wrong: one label.
- Ruling R52: InfoStrip loses its card and its time chips/yield line (the facts row carries times in both page and modal; showTimes prop removed); dietary line, an Eyebrow "Per serving · approx." + four tiles in one row from sm (2x2 on phones; bg-parchment ring-1 ring-line rounded-[3px], Newsreader 24px value over a 12.5px label), tags and flavour compass all stay — configurator .nutri — cost if wrong: layout of one block.
- Ruling R53: step rows keep the checkbox; numeral span per plan; step and ingredient rows gain configurator hairlines (border-b border-line), group headings Newsreader italic font-normal muted (both lists, configurator .ing h4 / .steps .sg) — cost if wrong: row spacing changes the measured column heights (verified by the gallery probe).
- Ruling R54: SupplementarySections panels become paper (rounded-[3px] bg-surface ring-1 ring-line, no shadow), headings font-heading font-normal text-brown-dark (headline weight 400; icons kept in terracotta); Tips = tinted box bg-parchment-dark rounded-[3px] px-5 py-4 with an Eyebrow label "Tips from the kitchen" (as h2) — spec §8 + §14 — cost if wrong: back-matter look.
- Ruling R55: gallery placement is verified by the controller with probe-gallery.mjs against the baseline above (page + modal, 1280/1024/768/390). If it differs, a fix round tunes the geometry (e.g. plate outside the measured section) before the PR; if it cannot match without breaking the spread, the difference is reported to the user — user's explicit invariant — cost if wrong: extras move between margin and band.
- Ruling R56: RecipeModal: controls become paper pills (bg-surface ring-1 ring-line text-brown-dark, no blur), grabber bg-brown-light/60; RecipeDetail in the modal gets top padding so the eyebrow clears the controls; stale heroBleed comments rewritten — "overlays are paper, never glass" — cost if wrong: modal chrome.
- Ruling R57: CookModeEntry = Button variant="primary" "Start cooking" (visible name; old aria-label "Enter cook mode" dropped so the name matches the label) + the hint "Step by step and hands-free. The screen stays awake." beside it in muted 13px; its whileInView fade kept — cost if wrong: smoke selectors, copy.
- Ruling R58: back link "All Recipes" → "All recipes" (sentence case) — §14 — cost if wrong: none.
- Ruling R59: run Phase 5 as ONE implementer dispatch (commit 5.1, commit 5.2) and one task review over the range, like R43; implementer on opus (RecipeDetail is load-bearing and multi-file), reviewer sonnet — cost if wrong: a larger review diff.
- Ruling R60: accept the new placement (all three extras in the margin from md; band only on phones) rather than force the baseline — the equal two-page spread makes the Method column narrow and tall, so the band placement cannot be restored without shrinking the Method page or lying to the hook; the hook's own rule (and the user's saved preference) is margin first, band as last resort. Surface to the user as a visible change, not a silent one. Fold the hook width fix (colWidth = clientWidth minus horizontal padding) into the review fix round — cost if wrong: the user wanted identical placement; restoring it needs the old 340px/flex column ratio inside the raised page.
- Ruling R61: fix round 1 (resume implementer) = (a) R60 hook width: colWidth = clientWidth minus computed horizontal padding; (b) the two Eyebrow-as-h2 sites render as <h2><Eyebrow as="span" className="block …">…</Eyebrow></h2> so the label keeps Hanken 600 + 0.16em tracking; (c) EquipmentList's label (font-heading semibold caps + icon, now in the header zone) becomes the same Eyebrow-in-h2 pattern with the icon kept, for one label style across the header — cost if wrong: one label style.
- Ruling R62: restore yieldText as a raw muted 13px line under the Ingredients header row (no "Makes " prefix: most authored strings already start with a verb/count) — §13/no functionality lost — cost if wrong: line placement; it sits in ingredientsRef so the gallery probe reruns.
- Ruling R63: add a `paprika` tone to Eyebrow and use text-paprika for the tips label and numerals (5.23:1 day, 4.86:1 night) — AA — cost if wrong: a slightly deeper terracotta in one box.
- Ruling R64: page padding p-5 sm:p-8 lg:p-14 on the full page; in the modal p-5 sm:p-8 (the 880px sheet never gets lg room) — departs from the plan-literal sm:p-14, spec silent, reading comfort on tablets and in the modal wins — cost if wrong: less configurator-like breathing room at md; gallery probe reruns.
- Ruling R65: fold these minors into the same wave: gutter drawn on the grid (1px line background, section border-l dropped); dead leading utilities removed (title keeps the global 1.15); facts-row heart matched to the sm Button height; nutrition tiles bg-surface; margin/band gallery figures rounded-[3px], no shadow (spec "only restyled"; heights unchanged so the hook model holds); displayAmount returns '' for amount 0 (copy text drops the empty amount, no double space); DropCap keeps the whole text for screen readers (visual cap aria-hidden + sr-only full text or equivalent); loading.tsx skeleton redrawn as title-on-paper + raised two-page sheet — cheap, the user reviews visually and by keyboard — cost if wrong: a few class strings; "0" and DropCap were pre-existing, fixed opportunistically.
- Ruling R66: keep the plan workspace/ledger (as R47), phases 6-9 remain — cost if wrong: a few KB of gitignored scratch.
- Ruling R67: the low-density blend floor in getChoroplethColor moves to the LIGHT constants (day #D6E6E3 = 214,230,227; night #183A3C = 24,58,60), derived from CHOROPLETH_LIGHT / SEPIA_CHOROPLETH_LIGHT rather than new magic numbers, so zero-recipe and low-density regions sit on one continuous teal ramp; comments rewritten — spec §9 "constants move from cobalt to teal" is the intent, a cobalt floor would tint every low-density region blue — cost if wrong: low-density regions read a touch paler/darker; one function.
- Ruling R68: the mobile map chrome glass (WorldMapMobile top pill, bottom bar, "list" pill; MapCoachmark) becomes paper: opaque bg-surface, border-line hairline, no backdrop-blur — Global Constraint "Overlays are paper, never glass" (same call as R56 for the modal) — cost if wrong: the map no longer shows faintly through the chrome; a class string each.
- Ruling R69: the controller verifies the night hatch by screenshot with a stubbed cooked country (dev-only page state or a devtools injection of the pattern), since Turnstile never issues an automated session; the user hand-checks the real hatch after logging a cook — cost if wrong: the real hatch is only eyeballed by the user.
- Ruling R70: Phase 6 = one implementer dispatch (sonnet: ~6 files, class-level judgment, no architecture), one task review (sonnet), final review (opus) — cost if wrong: none.
- Ruling R71: give the SVG map labels a cartographic halo: stroke var(--color-parchment) (theme-aware ground colour), strokeWidth ~3, paintOrder "stroke", strokeLinejoin round, on continent + region + country text in both WorldMapDesktop and MobileMapCanvas — keeps the spec's night ramp (#8CC3C1) and brown-dark ink, adapts in both themes with no JS theme check — cost if wrong: labels get a soft outline by day too (subtle on mist).
- Ruling R72: night map ground becomes the page colour: [data-theme="sepia"] { --color-map-base: var(--color-parchment) } (#122F31), so no-recipe land (#183A3C) sits a step lighter than the ocean, mirroring day; map-base is used only by the atlas and /dev/floating-navbar — spec §9 names #122F31 the night page and #183A3C an inset; resolves the §3/§9 contradiction toward §9 — cost if wrong: the night ocean is a shade darker than before.
- Ruling R73: fold the deferred hint-dismiss hover into the same fix wave as hover:bg-parchment/15 (theme-aware, one token) — cheap, reviewer-suggested — cost if wrong: none.
- Ruling R74: keep the plan workspace/ledger (as R47/R66), phases 7-9 remain — cost if wrong: a few KB of gitignored scratch.
- Ruling R75: "before" shots taken from :3000 at branch creation (HEAD == main 3ea132e, no commits yet) instead of a separate main worktree on :3001 — identical code, saves a second dev server — cost if wrong: none; shots are before-pantry-{1280,390}[-night], before-pantry-first-*-night (--dom). Configurator reference: config-pantry-{shelf,cook}-{day,night}-{1280,390} (shoot-config-pantry.mjs, Tweak panel hidden).
- Ruling R76: restructure the shelf to the configurator (plates=ruled): accordion on the left (1.2fr; ink top rule; serif kind name ~21px + muted count + chevron; line under each row), the OPEN kind reveals a ruled plate grid (grid border-t border-l border-line, cells border-r border-b border-line; art + serif name + muted kind; selected = inset 2px terracotta ring; repeat(auto-fill,minmax(140px,1fr)), 2 columns on phones); reading panel on the right (.8fr) — spec §10 "ruled grid of plates" is binding and the plan's cells don't exist without it — cost if wrong: the biggest visible change in the phase (text list → art plates); revert is one component.
- Ruling R77: PropheticSeal.tsx left untouched (no live caller; /dev/pantry imports it); phase 9's sweep decides its fate — cost if wrong: one dead-in-production file lingers until phase 9.
- Ruling R78: phones keep the SpreadOverlay (tap a plate → overlay) rather than the configurator's stacked panel; the overlay becomes paper (bg-surface, 1px line ring, 3px corners, no rounded-2xl) — §13 "entry overlay" must survive — cost if wrong: phones differ from the configurator's stacked reading panel.
- Ruling R79: the "Sunnah" word label (Hanken 10.5px semibold, tracking 0.12em, uppercase, terracotta; terracotta-lit inside the night passage) replaces the Keystone diamond as the Sunnah mark everywhere on /pantry (plates top-right, checklist rows right, filter chip, passage label); Keystone and the passage's notch diamond go — configurator sunmark=word + spec §10 — cost if wrong: the terracotta diamond motif leaves the pantry.
- Ruling R80: reading panel (desktop) = bg-surface, 1px line ring, 3px corners, p-8, md:sticky below the nav; its art sits on bg-plinth rounded-[3px] at 120px in BOTH themes; only the shelf plates use .ink-plinth (transparent by day, plinth at night) — spec §10 — cost if wrong: none expected.
- Ruling R81: the Sunnah filter chip keeps the night-fill pressed state (spec §5 chip rule, and it is the live behaviour) rather than the configurator's terracotta ring — spec > configurator — cost if wrong: a one-class change.
- Ruling R82: pantry headings go to weight 400 (h1, entry name, results heading, list titles no semibold) — spec §4.1 — cost if wrong: none.
- Ruling R83: drop all three phase-1 bridges in app/globals.css (and rewrite the bridge comment) because PantryShelf stops using text-brass / bg-brass / text-olive / bg-cobalt-deep — the comment itself says they retire in phase 7 — cost if wrong: a dev-sandbox (/dev/*) caller using text-olive at night loses its lift.
- Ruling R84: h1 "The Pantry" → "The pantry" and the lead/metadata description drop "The sealed ones…" for the configurator's lead "The ingredients I cook with most, what each one does, and which recipes use it." — sentence case rule + the seal no longer exists — cost if wrong: copy the user may want back; flagged in the report.
- Ruling R85: the prophetic passage = bg-night (#1B4345) by day, deepened at night to the configurator's #0B2224 via one [data-theme="sepia"] rule on a named class (e.g. .pantry-passage), so it separates from the night surface (#16373A); note text cream in Newsreader ~17.5px, label "From the Prophet's ﷺ table" cream semibold with the Sunnah label, citation cream/80 sans 13px (text verbatim) — configurator .passage — cost if wrong: one hard-coded night literal.
- Ruling R86: behaviour is unchanged (one kind open at a time, expanding does not change the selection, cook-mode scoring and copy unchanged, pick() still sets both openSlug and overlaySlug); the "The list / Open a shelf, then pick a line to read it." helper leaves (the configurator drops it and "pick a line" no longer describes plates) — look-and-feel phase — cost if wrong: helper copy to restore.
- Ruling R87: Phase 7 = one implementer dispatch (sonnet: one component + globals + page shell, explicit class-level brief), one task review (sonnet), final review (opus) — cost if wrong: a round of fixes if sonnet misreads the configurator.
- Ruling R88: accept the implementer's !text-parchment Sunnah label inside the pressed filter chip — terracotta #B4532E on #1A2B2D is ~2.9:1 by day and terracotta-lit on the inverted mist fill at night is ~1.9:1; parchment reads in both — cost if wrong: the label loses its terracotta only while the filter is on.
- Ruling R89: C1 fixed in this phase with the configurator's fallback: a featured slug with no landed entry displays as its capitalised slug (beef → "Beef", olive-oil → "Olive oil") — the configurator does exactly this and "undefined" is user-facing — cost if wrong: names an ingredient that has no pantry page yet.
- Ruling R90: C2/C3 enter the fix round with the reviewer's findings (reserve room for the X beside the art; let result titles wrap instead of truncate) — cost if wrong: none.
- Ruling R91: fix I1 by deriving the effective open kind: when expandedKind is non-null but no longer in byKind, fall back to byKind[0] (null still means the cook collapsed everything) — configurator logic; a revamp shouldn't ship an empty-looking shelf — cost if wrong: turning the filter on may open a different shelf than the one the cook had open.
- Ruling R92: fold two Minors into the round (comment hex; CookWith meta uses MetaDot like cook-mode rows); defer the Eyebrow tone override and the 130/140px minmax — cheap consistency — cost if wrong: none.
- Ruling R93: ONE final fix wave: F1 (md:grid-cols-[minmax(0,1.2fr)_minmax(0,.8fr)] + CookWith title wraps, clearing the deferred 390 truncation), plate + ReadingArt alt="" (decorative; the name is visible text), drop the dead panelRef, humanizeSlug to sentence case (R89's literal example) — all one-liners reviewers named — cost if wrong: none.
- Ruling R94: park the phone default-selection aria-pressed (same visual highlight as main; fixing needs a breakpoint-aware selection) and the sticky-panel-taller-than-viewport (panel is usually taller than the accordion so it rarely sticks; an inner scroll would be worse) — cost if wrong: a screen-reader user on a phone hears Barley as pressed before tapping; a long kind on a short laptop screen keeps "Cook with it" below the fold until the column ends.
- Ruling R95: keep the plan workspace/ledger (as R47/R66/R74), phases 8-9 remain — cost if wrong: a few KB of gitignored scratch.
- Ruling R96: "before" shots from :3000 at branch creation (HEAD == main f6a2439, no commits) — identical code, as R75 — cost if wrong: none. Shots: before-devjournal-{1280,390}[-night], before-journal-* (no session: failure/Turnstile state), before-*-first-*-night (--dom). Configurator: config-journal-{day(bare),night(plinth)}-{1280,390} via shoot-config-journal.mjs.
- Ruling R97: reorder JournalScrollView to spec §11: masthead → Titles (JournalRank) → The log → Stamps collected → Where next — spec binding, plan silent — cost if wrong: one JSX reorder.
- Ruling R98: keep JournalRank's own nextTierPhrase ("4 more countries and 1 region to <b>Globetrotter</b>.") instead of lib/passport progressToNextTier ("stamps") — it is the existing journal copy and matches the configurator; spec §11 names "country + region needs" — cost if wrong: copy swap.
- Ruling R99: one row per meal stays (spec "one row per meal"; live derivation untouched, no "Show N earlier" collapse); the row's second line becomes "<Country>. <Note>" with the note's first letter capitalised ("Turkey. Cooked again.", "Iraq. Your first from Iraq."), country alone when there is no note, note alone for origin-less dishes. lib/journal.ts marginNote strings unchanged — cost if wrong: copy tweak in one component.
- Ruling R100: Log row = configurator grid [60px mark | dish (Newsreader 21px, 19px phones) + note line (13.5px muted) | italic Newsreader date "22 Sept"-style right, 15.5px, nowrap]; phones: [50px | 1fr], date under the dish. JournalDishMark's passport-light bg-parchment tile → ink-plinth rounded-[3px] (no tile by day, plinth at night: the global ink-art rule) — cost if wrong: the log loses its parchment chips by day.
- Ruling R101: drop `recap` from JournalScrollView props, JournalScroll, useCookedStamps' return and /dev/journal; delete JournalJourney.tsx; keep lib/journal.ts buildJourneyRecap/JourneyRecap exports for phase 9's sweep; no top-region line (optional in spec, absent in configurator) — cost if wrong: a dead export lingers until phase 9; top region can be added as one masthead line.
- Ruling R102: section heads for Titles / The log / Stamps collected = configurator .sh: h2 Newsreader 400 ~25px, flex justify-between baseline, pb-2.5 border-b border-teal, right-hand small muted sans 13.5px count ("countries and regions cooked"; "N meals"; "N countries, M regions", singular-aware) — replaces the mono uppercase heads — cost if wrong: head style swap.
- Ruling R103: Rank = configurator .contents: ol of five, rows flex baseline gap-2.5 py-[11px] border-b border-line, serif 21px name, dotted muted leader (hidden on phones where the requirement wraps under the name), requirement right 14px muted: "Where everyone starts" for tier 0, else "N countries, M regions"; past names muted; current: 9px terracotta diamond before the name + requirement "Your title" terracotta semibold; ahead rows opacity-60; then the toward line (15px muted, next title ink semibold). Meters and the title trail go. Top tier keeps the live closing line — cost if wrong: visual only.
- Ruling R104: masthead = muted Eyebrow "Nieves's Kitchen", h1 font-normal clamp(2.6rem,5.4vw,4.2rem), "Kept since" italic Newsreader 17px muted, stats = Newsreader font-light 40px (32px phones) tabular numerals over lowercase singular-aware labels ("meals cooked"/"meal cooked", "dishes"/"dish", "countries"/"country"), right-aligned by day desktop / left on phones, 1px ink (border-brown-dark) rule under the masthead. Numerals weight 300 per configurator (not a headline) — cost if wrong: one class.
- Ruling R105: add a `muted` tone to the Eyebrow primitive (text-brown-medium, theme-aware) for the masthead kicker and region labels; month labels use the default terracotta tone (configurator .eb.month) — also retires phase 7's deferred "!text-brown-medium over Eyebrow" pattern for new code — cost if wrong: one more tone on a shared primitive.
- Ruling R106: --stamp-size = clamp(80px, 22vw, 107px) (not the plan's clamp(120px,34vw,150px)) so a square image stamp (1.4 × size) tops out at ~150px and phones fit two ~150px cells — spec §11 numbers are binding; the plan's value forgot CountryStampSlot's 1.4× multiplier — cost if wrong: stamps one notch smaller than the plan wanted.
- Ruling R107: Stamps collected = configurator ruled rows: each region a row (grid [170px | 1fr] desktop, stacked on phones) with border-b border-line; region label a muted Eyebrow; cells grid repeat(auto-fill,minmax(190px,1fr)) (150px on phones), each cell border-l border-line (border-r on phones), centered stamp + Newsreader 18px country name + "N dishes" muted (unique recipe slugs from stampsPerCountry). The CountryStampSlot button stays the single interactive target (its aria-label unchanged); the name/count are plain text. "See your world on the atlas →" below the rows, text-teal — cost if wrong: visible additions (names, counts) the plan did not list; flagged in the report.
- Ruling R108: .ink-plinth-panel (CSS only): by day no background and no padding; under [data-theme="sepia"] background var(--color-plinth), rounded-[3px] visible, padding ~ 4px 20px 20px, and locked day tokens: --color-brown-dark #1A2B2D, --color-brown-medium #4E6366, --color-brown-light (day value), --color-terracotta #B4532E, --color-teal #337677, --color-line #CAD9D6 (plus the .text-teal night lift must not win inside the panel) — the panel's link and hairlines would otherwise use night values on warm paper — cost if wrong: one CSS block.
- Ruling R109: JournalWhereNext restyled to the configurator ruled line (border-t border-brown-dark, border-b border-line, py-[18px], flex wrap baseline: terracotta Eyebrow "Where next?", Newsreader 21px dish, muted reason, teal underlined "Open recipe" link pushed right (left on phones)); reason copy unchanged; still one card and null-safe — cost if wrong: a visible change the plan didn't list, flagged.
- Ruling R110: Phase 8 = ONE implementer dispatch (sonnet) for 8.1+8.2 with a class-level brief (p8-brief.md) + p8-constraints.md, one sonnet task review, opus final review — as R87 — cost if wrong: a fix round if sonnet misreads the configurator.
- Controller smoke-p8: 131/135 first run; the 4 fails were harness assumptions → Ruling R111: "stamp up to 150px" (spec §11) is the square-equivalent size, because CountryStampSlot keeps constant area across aspects (Brazil 168×133 = 149.5² ); two-per-row on phones checked at a 470 viewport where the sandbox panel (340px) matches live /journal at 390 — cost if wrong: a landscape stamp is ~168px wide. After the fix: 135/135.
- Ruling R112: fold two Minors into round 1 — a shared JournalSectionHead (h2 + small count; carries the night rule class) replaces the three heads, and one plural helper lives with it — R102 asked for one shared head, and it fixes C2 in one place — cost if wrong: none.
- Ruling R113: log dates show the year only when it differs from the current year ("22 Sept" / "22 Sept 2025") — a long-lived journal would otherwise show identical dates across years; configurator only shows current-year data — cost if wrong: a slightly longer date on old rows.
- Ruling R114: ONE final fix wave takes F1 (CLAUDE.md lines 63 + 69 rewritten to the shipped journal: order, Journey cut, the title ladder as a ruled contents list showing both needs, no meters), F2 (--stamp-size clamp(80px,22vw,92px) below sm, 107px cap at sm+), F3, F5, F6 ("2 more regions" when the country need is met), F7 (inline lineHeight 1 on the h1) — all one-liners the reviewer specified — cost if wrong: none; CLAUDE.md edits are the user's project instructions, flagged in the report.
- Ruling R115: accept the challenge to R113 and revert to always "22 Sept" (month labels "September 2025" already carry the year, so the row year only added length) — reviewer's reading is right; I missed that the month header disambiguates — cost if wrong: none.
- Ruling R116: park — with F3's larger "Open recipe" (text-base), a long reason line ("A new region for you, East Asia. One dish opens a whole corner of the map.") pushes the link onto its own right-aligned line at ~900px; the configurator's shorter reason fits on one line. No second fix wave (one final wave per process) — cost if wrong: one class (text-sm) or a shorter reason if the user dislikes the wrap.
- Ruling R117: keep the plan workspace/ledger (as R95), phase 9 remains — cost if wrong: a few KB of gitignored scratch.
- Ruling R118: "before" shots from :3000 at branch creation (HEAD == main 97267c4, no commits), full Step 4 list + /dev/journal, day + night — identical code, as R75/R96 — cost if wrong: none. Shots: before-p9_{,recipes,recipes_turkish-eggs,recipes_xinjiang-lamb-dumplings,atlas,pantry,journal,favorites,promise,about,dev_journal}-{1280,390}[-night]; all no overflow.
- Ruling R119: delete Arch, CourtyardCard, DestinationTile, Filmstrip, StepTile, TileGrid, TilePattern (+ index.ts exports); trim app/dev/courtyard-kit/page.tsx to the survivors (Button, Chip, DropCap, Eyebrow, RuleDiamond); leave app/dev/redesign/v2 untouched (its look-alikes are local); rewrite the globals.css comments that name the deleted pieces — spec §12: any left with no importer outside app/dev is deleted, dev sandboxes trimmed — cost if wrong: git revert of one commit restores them.
- Ruling R120: drop the Eyebrow `olive`, `brass` and `cream` tones (no callers left after R119; courtyard-kit:71's olive → muted) — theme-stable text tones are exactly the night-contrast trap Step 2 hunts, and `muted` already covers olive's role — cost if wrong: a future caller re-adds one line.
- Ruling R121: delete components/pantry/PropheticSeal.tsx and lib/journal.ts buildJourneyRecap + JourneyRecap (+ any helper only they use) — zero importers on fresh grep; app/dev/pantry has its own seal — cost if wrong: none (git history).
- Ruling R122: leave the `turmeric` live call sites and the spec-§5 cobalt-deep Button hover / Chip active as they are — turmeric is the theme-aware terracotta by value with sepia text pins keyed on `.bg-turmeric` (renaming to terracotta would escape those pins: light ink on light terracotta at night), and CLAUDE.md "token names lie" forbids name-driven fixes; StampsUnavailable's `hover:bg-cobalt/[0.06]` → `hover:bg-brown-dark/[0.06]` (theme-aware ink tint; teal at 6% is invisible on night paper) — cost if wrong: a later rename sweep needs the pins moved first.
- Ruling R123: brown/warm shadow rgba(60,40,20|60,30,15|62,39,35,…) on live pages → neutral rgba(0,0,0,…) at the same alpha (plan S2's own rule); confetti fallback palette → Glazed Folio literals ['#B4532E','#F0A988','#B9CBC7','#337677'] (no yellow) — cost if wrong: shadows a touch cooler.
- Ruling R124: repoint the `.passport-light, .passport-paper` token lock in globals.css at the Glazed Folio DAY values (parchment #F4F7F6, parchment-dark #E3ECEA, surface #FAFCFB, surface-alt #E3ECEA, terracotta #B4532E, turmeric #B4532E, paprika #9E4527, sage #B9CBC7, teal #337677, brown-dark #1A2B2D, brown-medium #4E6366, brown-light #8BA19E, and add --color-line #CAD9D6); stamp-ink vars unchanged; `.passport-cover` sepia block (parked booklet only) left — the lock is still right (StampedRecipesModal shows ink stamps, which need light paper at night), but its values are the pre-revamp palette incl. yellow #D4A843, a spec §3.3 / "no yellow anywhere" violation on a live surface — cost if wrong: the stamp modal (and the parked booklet in /dev/passport) turn from warm cream to mist; VISIBLE, flagged in the report.
- Ruling R125: /promise: h1/h2/h3 → font-normal (keep sizes), CTA "Browse the recipes" → `<Button variant="primary" href="/recipes">` (rounded-sm, spec §5); the status pills stay rounded-full (chips); IFANCA/IslamQA source titles keep their em dashes (they are citation titles, verbatim rule); /about: h1 → font-normal — plan S2 — cost if wrong: none.
- Ruling R126: headline weight 400 (spec §4.1) on display headings of live surfaces: FilterPanel "Filters" h2, StampedRecipesModal h2 + its recipe titles (serif list names match the journal's 400 rows), StampsUnavailable h2 (both sizes), CookModeHero title, FullStoryDropdown toggle, atlas sidebar h3 (Desktop + Mobile); leave small UI labels (≤17px sans-scale serif labels: atlas h4 recipe names, panel titles, SupplementarySections dt, CookedButton toast lines, the phase-2 Footer wordmark) — spec §4.1 is about headlines — cost if wrong: a few labels read lighter than the user expects; VISIBLE, flagged.
- Ruling R127: close three phase-7 minors: GoodForYou → `<Eyebrow as="span" tone="muted">` (drop the ! override); PantryShelf plate grid minmax(130px) → minmax(140px) (configurator .plates); humanizeSlug → sentence case ("Olive oil", R89) — cost if wrong: plate columns change at some widths (VISIBLE, flagged).
- Ruling R128: R116 stays parked with no change — the configurator's .next rule (flex-wrap, margin-left:auto) wraps a long reason the same way; the live copy is data-driven — cost if wrong: one class (text-sm) if the user dislikes the wrap.
- Ruling R129: docs: CLAUDE.md "Nieves Kitchen" → "Nieves's Kitchen" (31, 241); Design Context Theme bullets name the real accents (terracotta/sage/teal; turmeric dropped since it IS terracotta); Principle 4's pairing → "Newsreader + Hanken Grotesk, with Cutive Mono for postal/stamp accents"; README.md + TODO.md titles/opening → "Nieves's Kitchen"; the design-language doc gets a top pointer to the premium-revamp spec (the live visual spec for colour/type/components; Courtyard doc kept for voice, layout and history) and §9 Imagery (full-bleed only for the home hero painting; recipe photos stay contained plates, text never on photos); the Journal section and Routing row are NOT touched (already current) — cost if wrong: doc edits, user's instructions file flagged in the report.
- Ruling R130: Phase 9 = ONE implementer dispatch (sonnet) with p9-brief.md + p9-constraints.md, one sonnet task review, opus final review — as R87/R110 — cost if wrong: a fix round.
- Ruling R131: smoke-p3 harness drift fixed — Next 15.5 `priority` emits a <link rel=preload as=image imagesrcset> for the hero instead of a fetchpriority attribute on the <img>; the check now accepts either. The remaining p3 fail ("card opens modal", 2.5s wait) is the dev server's first-click compile of the @modal route: a probe shows the modal opening in 0.9-1.4s warm; nothing on this branch touches home/cards/RecipeModal — cost if wrong: a real modal regression hides behind "flake" (the user's by-hand check covers it).
- Ruling R132: accept the R124 challenge — drop `passport-light` from StampedRecipesModal's panel so it follows the theme (night paper at night, like RecipeModal/FilterPanel); every token inside is theme-aware and the sepia border pins exist; keep the repointed .passport-light block (no yellow) for the parked booklet — verified: the modal imports no stamp component — cost if wrong: the modal goes dark at night (VISIBLE; before this branch it was a warm cream slab at night), one class to restore.
- Ruling R133: ONE final fix wave takes F1-F4 (reviewer's exact wording, README scope widened to the one stale sentence since this branch edited that line) + R132; F5 left (dead CSS outside the rulings, harmless) and listed as found-not-fixed — cost if wrong: none.
